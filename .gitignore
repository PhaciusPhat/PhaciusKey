# Rust
/target/
**/target/
Cargo.lock

# macOS
.DS_Store

# Claude Code — skills are shared, local permission grants and scratch are not
.claude/settings.local.json
.claude/.cc-writes/

# RustRover / JetBrains IDEs
# .idea/ holds generated project state: modules.xml, vcs.xml, the .iml module
# file, and workspace.xml (window layout, run history, per-user paths).
.idea/
*.iml
*.iws
*.ipr

# Brainstorm companion
.superpowers/

# Packaged app + installer
/dist/

# Upstream clones kept locally for reference only
/reference/

# Local release-signing material (cert, p12, keychain) — NEVER commit
.signing/
